# Codex 前端改动清单 — 第二轮细节修改

> WorkBuddy 后端已改完，Codex 请按此清单修改前端。
> 完整方案见 `docs/第二轮细节修改方案.md`

---

## 后端已改动（供联调参考）

| 文件 | 改动 |
|------|------|
| `backend/src/services/ai_service.py` | `analyze_mood_multi_modal` 增加 30s 超时 + 1 次重试 + logging；新增 `generate_companion_memories()` AI 语义记忆 |
| `backend/src/api/ai.py` | `analyze_mood_endpoint` 返回 `fallback` 字段；`get_companion_memories` 优先 AI 记忆，响应新增 `source` 字段 |

### 新增响应字段
- `POST /api/ai/analyze-mood` → 新增 `fallback: true/false`
- `GET /api/companion/memories` → 新增 `source: "ai" | "rules" | "empty"`

---

## Codex 需要改的前端文件（6 个）

### 1. `frontend/src/app/music/page.tsx`
- [ ] 加载状态优化：区分"正在生成"、"网络异常"、"使用本地模板"三种状态
- [ ] 读取 `response.data.fallback` 字段，fallback=true 时展示友好提示"AI 伙伴暂时离线，先看看本地建议～"
- [ ] 确认"重新生成陪伴语"按钮正常工作

### 2. `frontend/src/app/companion/page.tsx`
- [ ] **移除** 第 116 行 fallback 提示文字 `"记忆接口暂时不可用，先展示本地示例记忆。"`（第 116 行 `setMemoryNotice` 调用）
- [ ] 记忆 Tab 增加加载骨架屏（用已有的 `Loader2` + placeholder 卡片）
- [ ] 记忆为空时展示引导文案："伙伴还没有形成稳定记忆。多记录几次心情后，这里会慢慢长出你的专属线索。"（已有，确认保留）
- [ ] 根据 `payload.source === "ai"` 展示「AI 生成」小标签
- [ ] 对话 Tab 发消息时增加打字动画效果（逐字显示，可用 CSS animation 或逐帧 setState）

### 3. `frontend/src/app/discovery/page.tsx`
- [ ] 左栏改为 `lg:max-h-[calc(100vh-7rem)] lg:overflow-y-auto` + `lg:sticky lg:top-24`
- [ ] 右栏改为 `lg:max-h-[calc(100vh-7rem)] lg:overflow-y-auto`
- [ ] 左栏内容压缩：缩小"解忧角规则"卡片 padding，精简文案
- [ ] 移动端保持现有堆叠布局不变

### 4. `frontend/src/components/moodwave-shell.tsx`
- [ ] 第 45 行 fallback 昵称改为 `user?.username || "MoodWave 用户"`（与 profile 统一）

### 5. `frontend/src/app/profile/page.tsx`
- [ ] 第 128 行 fallback 昵称改为 `user?.username || "MoodWave 用户"`（与 shell 统一）
- [ ] `addChecklistItem()` 改为基于文本关键词推断情绪标签（复用 companion 页 `inferMood()` 逻辑，见 `companion/page.tsx:60-67`）
- [ ] 心境清单每项增加可切换标签的下拉按钮（用户可手动修正情绪标签），用 `<select>` 实现

### 6. `frontend/src/components/mood-media-upload.tsx`
- [ ] 响应式列数：`grid-cols-2 sm:grid-cols-3 lg:grid-cols-2 xl:grid-cols-3`
- [ ] 添加图片按钮增加 `min-w-[100px] min-h-[100px]`

---

## 共享工具函数（建议抽取）

`companion/page.tsx:60-67` 的 `inferMood()` 在 profile 页也需要用，可以直接复制或抽取到 `@/lib/utils`：

```typescript
export function inferMoodFromText(text: string): MoodType {
  if (/开心|顺利|高兴|快乐|兴奋/.test(text)) return "happy"
  if (/焦虑|紧张|考试|deadline|来不及/.test(text)) return "anxious"
  if (/生气|烦|火大|愤怒/.test(text)) return "angry"
  if (/难过|失落|哭|低落|累/.test(text)) return "sad"
  if (/平静|放松|还好/.test(text)) return "calm"
  return "neutral"
}
```

---

## 验证顺序

1. 改完前端 → `npm run build` 确认无编译错误
2. 启动后端 `uvicorn src.main:app --reload` → 测试 `/api/ai/analyze-mood` 返回 `fallback` 字段
3. 测试 `/api/companion/memories` 返回 `source` 字段
4. 联调：播放音乐 → 陪伴语是否正常生成 / 伙伴页对话是否流畅 / 记忆是否 AI 生成
