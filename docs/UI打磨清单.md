# MoodWave 前端 UI 打磨清单

> 审查时间：2026-04-29 22:12
> 问题来源：用户实际使用反馈 + 代码审查

---

## 一、问题汇总

### 🔴 P0 — 功能性缺陷（按钮无法点击/假网页感）

| # | 页面 | 问题 | 位置 |
|---|------|------|------|
| 1 | Landing | "查看原型首页" 按钮指向 `/dashboard`，未登录会被守卫踢回 `/login`，用户体验像坏链 | `page.tsx:72` |
| 2 | Landing | 右侧预览卡片中的 "🎤 说说此刻的心情"、"📊 我的趋势"、"🎵 治愈音乐"、"🤝 解忧角" 全是纯 `<div>`，无法点击跳转 | `page.tsx:117-131` |
| 3 | Landing | "78.1% 的大学生每周都会经历负面情绪" 徽章无交互 | `page.tsx:44` |
| 4 | Login | "忘记密码？" 按钮无任何功能 | `login/page.tsx:211` |
| 5 | Login | 微信/Apple/Google 第三方登录按钮无功能，点击无反应 | `login/page.tsx:240-277` |
| 6 | Dashboard | 通知铃铛按钮无功能 | `moodwave-shell.tsx:100-103` |
| 7 | Dashboard | "温柔模式进行中" 徽章无功能 | `dashboard/page.tsx:77-79` |
| 8 | Mood | 图片上传只选文件不真正上传 | `mood/page.tsx:200-215` |
| 9 | Mood | 语音输入只显示"语音入口已预留" | `mood/page.tsx:218-226` |
| 10 | Analytics | 月份前后翻页按钮无功能 | `analytics/page.tsx:186-195` |
| 11 | Profile | "个人信息"、"通知设置"、"主题设置"、"使用指南"、"关于我们" 5个设置项按钮无功能 | `profile/page.tsx:183-199` |
| 12 | Profile | 头像硬编码为 👧，用户名硬编码为"小鱼" | `profile/page.tsx:98-104` |
| 13 | Profile | "退出登录"按钮重复（侧边栏和设置列表各一个） | `moodwave-shell.tsx:79-86` + `profile/page.tsx:42` |

### 🟠 P1 — 开发痕迹文字泄露（像假网页）

| # | 页面 | 泄露文字 | 应改为 |
|---|------|----------|--------|
| 1 | Login | "移动端优先体验已经准备好，今晚的感受可以继续留在这里。" | 改为正常的登录引导语，如"登录后继续你的情绪之旅" |
| 2 | Dashboard | "展示 `GET /api/moods` 的最新记录，接口未就绪时用本地占位数据。" | 删除或改为"最近记录的情绪" |
| 3 | Dashboard | "这里先由前端根据时间与当前情绪做文案占位，后续交给 `workbuddy` 接接口。" | 删除 |
| 4 | Dashboard | "联调接口建议：`GET /api/moods` 返回最近 3 条记录；后续可补..." | 删除整个虚线框 |
| 5 | Mood | "文字先落地，图片和语音先保留前端入口给后续接口。" | 改为"用文字、图片或语音记录此刻" |
| 6 | Mood | "后续由 `POST /api/upload/image` 接入" | 删除 |
| 7 | Mood | "先完成 UI 和状态流，录音转文字由后续接口接入。" | 改为"语音功能即将上线" |
| 8 | Mood | "接口预留" 整个区块 + 3 条 `POST /api/...` | 删除整个侧边栏"接口预留"区块 |
| 9 | Mood | "提交已经完成。当前页面先展示前端占位分析，后续可由 AI 接口替换。" | 改为"情绪分析完成" |
| 10 | Mood | "准备就绪后会提交到 `POST /api/moods`，再进入分析结果。" | 改为"正在分析你的情绪波纹" |
| 11 | Analytics | "接口已连接" / "Mock 数据" 标签 | 删除此标签 |
| 12 | Music | "Canvas 粒子正在跟随情绪节奏呼吸" | 删除或简化 |
| 13 | Music | "来自 DeepSeek SSE 流式回复" | 改为"AI 陪伴建议" |
| 14 | Discovery | "接口不可用时使用本地数据。" | 改为"温柔回应，不评判" |
| 15 | Discovery | "已接入 `GET /api/posts`。" / "当前显示 Mock 数据。" | 删除或改为"实时更新" |

### 🟡 P2 — 布局/样式问题

| # | 页面 | 问题 | 建议 |
|---|------|------|------|
| 1 | Login | 登录/注册切换时表单高度不同（注册多了用户名输入框），导致整体内容区域跳动 | 统一表单高度，或用 `min-h` 保持一致，切换时平滑过渡 |
| 2 | Shell | 侧边栏退出登录在底部，内容多时需要滚动才能看到 | 退出按钮应 `sticky` 固定在侧边栏底部，不随内容滚动 |
| 3 | Mobile | 底部导航栏 4 个 tab 挤在一行，手机窄屏时文字截断 | 考虑缩减为图标+极简文字，或减少 tab 数量 |
| 4 | Profile | 头像和用户名硬编码，不读取 auth store 的真实数据 | 从 `useAuthStore()` 读取 username 和 avatar |
| 5 | Analytics | 日历格子和热力图在手机上太挤 | 小屏幕时简化日历显示 |

### 🔵 P3 — 图标/Logo 美化

| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 1 | Logo | 当前 logo 是用 CSS 画的几条渐变线（像音波），辨识度低 | 重新设计：用 🌊 emoji + 文字，或画一个更有辨识度的 SVG logo |
| 2 | PWA 图标 | 192/512 图标可能质量不佳（Codex 生成的） | 用 AI 生成更精致的图标，统一品牌视觉 |
| 3 | 侧边栏导航 | 用 emoji 作图标（🏠✏️📈🎵🤝👤）显得不够精致 | 换成 lucide-react 图标（已部分在底部导航使用） |
| 4 | Landing | 右侧预览卡片纯装饰，感觉像原型 | 让它可交互，或替换为真实截图/动图 |

---

## 二、分工方案

### WorkBuddy（后端+后端相关修复）

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | "忘记密码" 接口 | `POST /api/auth/forgot-password`（可先做占位返回"功能开发中"） |
| P0 | Profile 用户信息接口 | `GET /api/auth/me` 已有，确认前端正确调用 |
| P1 | 清理后端返回数据中的调试信息 | 确保 API 返回干净的业务数据 |

### Codex（前端UI修复，主力）

| 优先级 | 任务 | 涉及文件 | 说明 |
|--------|------|----------|------|
| **P0** | Landing 页可点击化 | `page.tsx` | ①"查看原型首页"改为"立即开始"→`/login` ②右侧卡片加 Link 跳转 ③去掉"78.1%"徽章的悬浮感或改为统计数据展示 |
| **P0** | Login 页假按钮处理 | `login/page.tsx` | ①"忘记密码"点击弹 toast"功能开发中" ②第三方登录按钮加 tooltip 或 disabled + "即将上线" |
| **P0** | Dashboard 去开发痕迹 | `dashboard/page.tsx` | ①删除 `GET /api/moods` 提示文字 ②删除"AI 每日寄语"的开发提示 ③删除虚线联调建议框 |
| **P0** | Mood 页去开发痕迹 | `mood/page.tsx` | ①删除"接口预留"侧边栏区块 ②清理所有 `POST /api/...` 文字 ③语音/图片入口改为"即将上线"提示 |
| **P0** | Analytics 去开发痕迹 | `analytics/page.tsx` | ①删除"Mock 数据"/"接口已连接"标签 ②月份翻页按钮至少切换月份显示 |
| **P0** | Discovery 去开发痕迹 | `discovery/page.tsx` | ①删除 Mock/API 连接提示 ②改为更友好的状态文案 |
| **P0** | Music 去开发痕迹 | `music/page.tsx` | ①删除"Canvas 粒子..." ②"DeepSeek SSE"改为"AI 陪伴" |
| **P1** | Profile 读真实用户数据 | `profile/page.tsx` | 从 auth store 读取 username/avatar，替换硬编码"小鱼/👧" |
| **P1** | Profile 设置项处理 | `profile/page.tsx` | 点击弹出 toast"功能开发中"，退出登录复用侧边栏逻辑 |
| **P1** | Login 登录/注册切换跳动 | `login/page.tsx` | 表单区域设统一 min-h，切换时平滑动画 |
| **P2** | 侧边栏退出登录位置 | `moodwave-shell.tsx` | 退出按钮 sticky 到侧边栏底部 |
| **P2** | 底部导航优化 | `moodwave-shell.tsx` | 手机端图标+短文字，确保不截断 |
| **P3** | Logo 重设计 | `moodwave-logo.tsx` | 用 🌊 emoji + 优雅排版，或重新设计 SVG |
| **P3** | 侧边栏 emoji 换图标 | `moodwave-shell.tsx` | 全部换成 lucide-react 图标 |
| **P3** | PWA 图标优化 | `public/icons/` | 用 AI 生成更精致的图标 |

---

## 三、修复原则

1. **不能点击的按钮必须给反馈**：要么实现功能，要么 disabled + 提示，绝不能点而无反应
2. **所有用户可见文字必须是产品文案**：零 API 路径、零技术术语、零"占位/Mock/接口"字样
3. **页面切换保持视觉稳定**：表单高度统一、过渡动画流畅
4. **用户数据必须真实**：头像、用户名从 auth store 读取，不硬编码
5. **图标统一精致**：不再混用 emoji 和 lucide，统一视觉语言

---

*此文档作为 Day 6→7 过渡的 UI 打磨指南，按优先级逐步执行。*
